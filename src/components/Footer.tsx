import React from "react";

export default function Footer({ classNames }: { classNames: string }) {
  return <div className={`footer p-4 bg-base-200 text-base-content ${classNames}`}>Footer</div>;
}
